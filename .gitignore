node_modules/
dist/
build/
# `tsc -b` writes this next to tsconfig.json to cache what it already compiled.
*.tsbuildinfo
# Local state, not source: the Postgres volume and uploaded avatars both live here.
.data/
.env
.env.local
*.log
.DS_Store
coverage/
.turbo/
